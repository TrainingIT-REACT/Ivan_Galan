import React from 'react';
import style from './favorites.module.css';

// import Card from '../card';

const Favorites = () => {
    return (
        <>
            <h5>Favorites Works!</h5>
            <div className={style.container}>
                {/* {albums.map( (album, i) => <Card data={album} key={i} />)} */}
            </div>
        </>
    );
};

export default Favorites;
